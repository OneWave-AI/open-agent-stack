# sand-terra ships no runtime secrets. If you serve web fonts behind a key
# or a private CDN, define placeholders here and load them from the environment.
# Never commit real values.

FONTS_CDN_BASE_URL=
FONTS_API_KEY=
