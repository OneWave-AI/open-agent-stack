# market-researcher environment
# Copy to .env and fill values. Never commit real secrets.
# Only needed if your runtime does not provide web search natively.

# Web search provider API key (e.g. Brave, Serper, Tavily, Bing).
WEB_SEARCH_API_KEY=

# Optional: search provider endpoint, if your client requires it.
WEB_SEARCH_ENDPOINT=
